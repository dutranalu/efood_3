# efood (EBAC) — Layout igual ao Figma

React + Vite + TypeScript  
Styled-components + React Router + Redux Toolkit + RTK Query

## Rodar
```bash
npm i
npm run dev
```

## Deploy (Vercel)
- Framework: Vite
- Build command: `npm run build`
- Output: `dist`
